import { describe, it, expect } from 'vitest';

// Placholder test to ensure the testing framework is set up correctly. This test should pass and show green in the test results.
describe('Configuración inicial', () => {
    it('El test de prueba debería pasar en verde', () => {
        expect(1 + 1).toBe(2);
    });
});